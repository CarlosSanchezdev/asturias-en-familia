# ─── Backend ──────────────────────────────────────────────
PORT=3000
MONGODB_URI=mongodb://mongodb:27017/asturias-familia

# JWT — cambia estos valores en producción
JWT_SECRET=change_me_in_production_please
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ─── Frontend ─────────────────────────────────────────────
ANGULAR_PORT=4200
API_URL=http://localhost:3000

# ─── Mongo (opcional, para auth en prod) ──────────────────
# MONGO_INITDB_ROOT_USERNAME=admin
# MONGO_INITDB_ROOT_PASSWORD=secret
